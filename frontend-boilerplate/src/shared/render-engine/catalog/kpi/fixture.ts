/** Fixture do bloco `kpi` — casa com o dataContract (shape 'scalar'). */
import type { ScalarData } from '@dashboards/contracts';

export const fixture: ScalarData = {
  value: 1284000,
  label: 'Total arrecadado',
  unit: 'BRL',
  delta: 0.12,
};
