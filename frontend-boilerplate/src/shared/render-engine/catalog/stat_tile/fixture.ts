/** Fixture do bloco `stat_tile` — casa com o dataContract (shape 'scalar'). */
import type { ScalarData } from '@dashboards/contracts';

export const fixture: ScalarData = {
  value: 8420,
  label: 'Eventos hoje',
  delta: 0.06,
};
