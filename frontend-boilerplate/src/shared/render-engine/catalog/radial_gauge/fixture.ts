/** Fixture do bloco `radial_gauge` — casa com o dataContract (shape 'scalar'). */
import type { ScalarData } from '@dashboards/contracts';

export const fixture: ScalarData = {
  value: 72,
  label: 'Cobertura da meta',
  unit: '%',
};
