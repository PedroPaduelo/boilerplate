/** Fixture do bloco `progress_circle` — casa com o dataContract (shape 'scalar'). */
import type { ScalarData } from '@dashboards/contracts';

export const fixture: ScalarData = {
  value: 75,
  label: 'Conclusão',
};
