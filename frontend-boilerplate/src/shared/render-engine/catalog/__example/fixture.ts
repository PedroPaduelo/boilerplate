/**
 * Fixture do bloco de exemplo. É um bloco narrativo (kind 'title', sem
 * `dataContract`) — portanto NÃO consome dados: a fixture de dados é `null`.
 * Blocos com dados (T-I) exportam aqui um exemplo que casa com o `dataContract`.
 */
export const fixture = null;
